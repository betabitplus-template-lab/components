# Pilot component update C2

This commit exists only to exercise the isolated component-to-template update chain.

Expected generated changes:

- `.editorconfig` gains `max_line_length = 100` in all three final templates.
- generated library README files mention versioned Copier releases.

Remove this marker in a later cleanup release if the architecture is adopted.
